# OptiMap
Cache-aware C++ map optimized for speed and efficiency.

### Prerequisites
- CMake 4.0 or higher
- A C++23 compatible compiler

## Benchmarking

### Running Benchmarks

```bash
# Build the project
mkdir build && cd build
cmake ..
make

# Run benchmarks
./OptiMapBenchmarks
```